/**
 * Расширения Telegram Mini Apps (@telegram-apps/sdk-react).
 * Хуки вроде useRawInitData / useLaunchParams вызывайте только внутри Mini App,
 * иначе возможны исключения при инициализации.
 */
export { useRawInitData, useLaunchParams } from '@telegram-apps/sdk-react'
